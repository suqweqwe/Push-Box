<Page x:Class="SokobanGame.MapSelectPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MapSelectPage">

    <Grid Background="LightSalmon">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="1" Grid.Column="1" Text="选择地图" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="DarkRed"/>

        <!-- 地图按钮将动态生成 -->
        <StackPanel Grid.Row="2" Grid.Column="1" x:Name="MapButtonsPanel" Orientation="Vertical" HorizontalAlignment="Center"/>

        <Button Grid.Row="3" Grid.Column="1" Content="返回" FontSize="20" Margin="10" Click="Back_Click"/>
    </Grid>
</Page>