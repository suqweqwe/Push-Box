<Page x:Class="SokobanGame.GamePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GamePage"
      KeyDown="Page_KeyDown"
      Focusable="True">

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="返回选择地图" FontSize="16" Margin="10" Click="BackToMapSelect_Click"/>

        <!-- 游戏区域使用Viewbox来自动缩放 -->
        <Viewbox Grid.Row="1" Stretch="Uniform" Margin="20">
            <Grid x:Name="GameGrid" ShowGridLines="False">
                <!-- 行和列将在代码中动态生成 -->
            </Grid>
        </Viewbox>

        <!-- 通关提示 -->
        <Grid x:Name="SuccessGrid" Visibility="Hidden" Background="Black" Opacity="0.8" Panel.ZIndex="100">
            <TextBlock Text="恭喜通关！" FontSize="72" Foreground="Yellow" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>